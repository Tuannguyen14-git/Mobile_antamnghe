import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF2962FF);
  static const primaryLight = Color(0xFF4F7BFF);

  // Background
  static const background = Color(0xFFF5F7FB);
  static const card = Colors.white;

  // Text
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFF9CA3AF);

  // Status
  static const success = Color(0xFF22C55E); // cho phép
  static const warning = Color(0xFFFACC15); // khẩn cấp
  static const danger = Color(0xFFEF4444); // chặn

  // Border
  static const border = Color(0xFFE5E7EB);
}
